# Screenshots (placeholders)

Capture after morning live run; attach to Google Doc.

1. **SSO claim page** — DID visible (redact key)
2. **`npm run auth`** — printed `did:t3n:…` *(live still blocked on trustAnchor — see BUGS.md)*
3. **`npm run demo`** — full `AgentDecision` JSON (`approve_full` fixture) — **stub capture in `docs/DEMO_LOG.md` (2026-09-13)**
4. **Optional** — deny fixture output

Stub-mode demo is screenshot-worthy for architecture review. Live auth remains blocked until Terminal 3 publishes testnet `trustAnchor` values; do not submit `unsafe_trust_server` as the production story.
